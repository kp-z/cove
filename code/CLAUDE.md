# Cove 代码库 · Claude Code 项目记忆

本文件是 Claude Code 在 `code/` 目录及其子目录下工作时自动加载的项目记忆文件。

完整的 AI 辅助开发规范（开发哲学、各子项目目录结构规范、代码风格、AI 行为准则）统一维护在同目录下的 `AGENTS.md` 中，避免规范内容在多处重复维护。下面通过 Claude Code 官方支持的 `@` 导入语法引入其全文，请将其视为与本文件同等权威的强制性开发规范：

@AGENTS.md

## Claude Code 专属提示

- 会话开始处理任何 `code/` 下的编码任务前，先确认已理解上方 `AGENTS.md` 中"二、目录结构规范"针对当前所在子项目（`local`/`cloud/backend`/`cloud/frontend`）的章节，再决定新文件应该落在哪个目录。
- 若任务需要产出说明性/总结性文档，遵循 `AGENTS.md` "2.5 文档存放规范"：放入仓库根目录 `doc/` 对应分类，不要在 `code/` 或子项目根目录下新建游离的总结型 md 文件。
- 修改规范本身时，只编辑 `AGENTS.md`，不要在本文件中重复或另行维护一份规则副本。
