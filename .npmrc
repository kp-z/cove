# NPM Configuration for Cove Project

# Prisma binaries mirror (for users in China or with slow GitHub access)
# Uncomment the line below if you experience slow Prisma engine downloads
# prisma_binaries_mirror=https://registry.npmmirror.com/-/binary/prisma

# Strict peer dependencies (recommended for consistent builds)
legacy-peer-deps=false
